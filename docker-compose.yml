services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: prepharbor
    ports:
      - "5432:5432"
    volumes:
      - prepharbor_pg:/var/lib/postgresql/data

volumes:
  prepharbor_pg:
